git clone -q https://github.com/anonymousx97/ux-jutsu u-x
cd u-x
PIP_ROOT_USER_ACTION=ignore
pip install -q -U pip
pip install -q -U setuptools
pip -q install --no-cache-dir -r requirements.txt
bash run
